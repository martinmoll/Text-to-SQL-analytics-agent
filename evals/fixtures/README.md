# Eval Fixtures

JSON question/answer pairs used for offline evaluation. Each fixture pins expected metrics, tables, filters, and ground-truth values to a snapshot date.

- `returns_evals.json` — Q&A pairs for return metrics
- `risk_evals.json` — Q&A pairs for risk metrics
- `fundamentals_evals.json` — Q&A pairs for valuation metrics
